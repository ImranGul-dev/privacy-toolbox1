import Link from 'next/link';
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const title = slug.replaceAll('-', ' '); return { title, description: `Privacy Toolbox guide: ${title}.` }; }
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const title = slug.replaceAll('-', ' '); return <main><section className="py-12 sm:py-16 lg:py-20"><div className="site-container"><article className="card mx-auto max-w-3xl p-8 sm:p-10"><span className="eyebrow">File privacy guide</span><h1 className="heading-lg mt-4 capitalize">{title}</h1><p className="lead mt-4">Learn how hidden file data works, how scan-clean-verify workflows reduce sharing risk, and which Privacy Toolbox tool is best for the file you want to send.</p><div className="mt-8 grid gap-4"><section className="rounded-2xl bg-slate-50 p-5"><h2 className="text-xl font-bold text-ink">What to check first</h2><p className="mt-2 text-sm leading-7 text-subtle">Start with the file type. Photos can contain EXIF, GPS, IPTC, and XMP data. PDFs can contain document properties, XMP, attachments, comments, forms, and scripts. Office files can contain author data, comments, tracked changes, hidden sheets, notes, and custom properties.</p></section><section className="rounded-2xl bg-slate-50 p-5"><h2 className="text-xl font-bold text-ink">Recommended workflow</h2><p className="mt-2 text-sm leading-7 text-subtle">Upload the file, scan for metadata, review detected categories, clean when needed, and verify the cleaned output before sending.</p></section></div><Link className="btn btn-primary mt-8" href="/tools">Open privacy tools</Link></article></div></section></main>; }
